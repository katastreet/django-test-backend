from django.urls import path

from .views import AppointmentView, AppointmentView2, SingleAppointmentView

app_name = "backend"

# app_name will help us do a reverse look-up latter.
urlpatterns = [

]
